# commands

```
$ ls
$ cd
$ mkdir
$ rmdir
$ rm
$ mv
$ cp
$ cat
$ nano
$ vi
$ grep
$ awk
$ sed
$ tail
$ head
$ which
$ whatis
$ where
$ whereis
$ users
$ groups
$ chmod
$ chown
$ stat
$ python run.py
$ sudo apt-get install
$ git init && git add . && git commit
$ systemctl
$ dpkg ./vlc.deb
```
